package org.example.cab302project;

import javafx.scene.control.Label;
import javafx.scene.layout.VBox;

public class TasksTab extends VBox {
    public TasksTab() {
        // Adding elements to the "Boats" tab
        getChildren().add(new Label("Tasks"));
    }
}
